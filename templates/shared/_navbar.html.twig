<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">TodoList</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">Accueil
                    <span class="sr-only">(current)</span>
                </a>
            </li>
        </ul>
        <ul class="navbar-nav">
            {% if app.user is not null %}
                <li class="nav-item"><a href="" class="nav-link">Bienvenue {{ app.user.username }} </a></li>

                {% if is_granted('ROLE_ADMIN') == true %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button"
                           aria-haspopup="true"
                           aria-expanded="false">Gestion Utilisateurs</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('user_create') }}">Créer un utilisateur</a>
                            <a class="dropdown-item" href="{{ path('user_show') }}">Voir les utilisateurs</a>
                            <a class="dropdown-item" href="{{ path('task_create') }}">Créer une tache</a>
                            <a class="dropdown-item" href="{{ path('task_show') }}">Voir les taches</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button"
                           aria-haspopup="true"
                           aria-expanded="false">Gestion Compte</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('user_profile') }}">Mon compte</a>
                            <a class="dropdown-item" href="{{ path('task_mytask') }}">Mes taches</a>
                            <a class="dropdown-item" href="{{ path('task_create') }}">Créer une tache</a>
                        </div>
                    </li>
                {% endif %}



                <li>&nbsp;</li>
                <li class="nav-item"><a href="{{ path('security_logout') }}" class="btn btn-danger">Déconnexion</a></li>

            {% else %}

                {#<li class="nav-item"><a href="{{ path('security_registration') }}" class="nav-link">Inscription</a></li>#}
                <li class="nav-item"><a href="{{ path('security_login') }}" class="btn btn-success">Connexion</a></li>

            {% endif %}
        </ul>
    </div>
</nav>